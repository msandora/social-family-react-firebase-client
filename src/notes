-View all global dependencies:
npm ls -g --depth 0

-Install firebase tools:
npm install -g firebase-tools
-Ahmed Uses:
firebase-tools@6.5.0
- login firebase:
firebase login:
- initialize firebase in a repo
firebase init
  >select functions
  >select project
  >select javascript
  >EsLint - No
  >Yes to install packages

